function LoginPage() {
    return (
        <div className="jumbotron">
            <h1 className="display-5 jol">Cricket League Application</h1>
         <hr/>
            <p >
            This project is React Single Page Application (SPA). This app will consume REST endpoints using axios module. This project will have several component based on user interaction the appropriate component will call and that setting is defined in App.js in the form of Router.
            Router will have several Routes defined, one Route for one component.
            </p>
        </div>
    );
}
export default LoginPage;